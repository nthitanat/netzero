# Load environment variables from .env in project root
# Use: docker-compose up
# The .env file will be automatically loaded

services:
  # Main API Server
  # Note: Database should be running on host machine (localhost:3306)
  netzero-server:
    build:
      context: ./netzero-server
      dockerfile: Dockerfile
    container_name: netzero-server
    restart: unless-stopped
    environment:
      DEPLOYMENT_MODE: ${DEPLOYMENT_MODE:-development}
      NODE_ENV: ${DEPLOYMENT_MODE:-development}
      # Development variables
      DEV_DB_HOST: ${DEV_DB_HOST}
      DEV_DB_PORT: ${DEV_DB_PORT}
      DEV_DB_USER: ${DEV_DB_USER}
      DEV_DB_PASSWORD: ${DEV_DB_PASSWORD}
      DEV_DB_NAME: ${DEV_DB_NAME}
      DEV_JWT_SECRET: ${DEV_JWT_SECRET}
      DEV_JWT_EXPIRES_IN: ${DEV_JWT_EXPIRES_IN}
      DEV_CORS_ORIGIN: ${DEV_CORS_ORIGIN}
      DEV_SURVEYMONKEY_ACCESS_TOKEN: ${DEV_SURVEYMONKEY_ACCESS_TOKEN}
      DEV_SURVEYMONKEY_SURVEY_ID: ${DEV_SURVEYMONKEY_SURVEY_ID}
      DEV_SURVEYMONKEY_SURVEY_REDIRECT_URL: ${DEV_SURVEYMONKEY_SURVEY_REDIRECT_URL}
      DEV_SURVEYMONKEY_WEBHOOK_SECRET: ${DEV_SURVEYMONKEY_WEBHOOK_SECRET}
      # Production variables
      PROD_DB_HOST: ${PROD_DB_HOST}
      PROD_DB_PORT: ${PROD_DB_PORT}
      PROD_DB_USER: ${PROD_DB_USER}
      PROD_DB_PASSWORD: ${PROD_DB_PASSWORD}
      PROD_DB_NAME: ${PROD_DB_NAME}
      PROD_JWT_SECRET: ${PROD_JWT_SECRET}
      PROD_JWT_EXPIRES_IN: ${PROD_JWT_EXPIRES_IN}
      PROD_CORS_ORIGIN: ${PROD_CORS_ORIGIN}
      PROD_SURVEYMONKEY_ACCESS_TOKEN: ${PROD_SURVEYMONKEY_ACCESS_TOKEN}
      PROD_SURVEYMONKEY_SURVEY_ID: ${PROD_SURVEYMONKEY_SURVEY_ID}
      PROD_SURVEYMONKEY_SURVEY_REDIRECT_URL: ${PROD_SURVEYMONKEY_SURVEY_REDIRECT_URL}
      PROD_SURVEYMONKEY_WEBHOOK_SECRET: ${PROD_SURVEYMONKEY_WEBHOOK_SECRET}
      # Shared variables
      PORT: ${PORT}
      API_PREFIX: ${API_PREFIX}
      API_VERSION: ${API_VERSION}
      UPLOAD_DIR: ${UPLOAD_DIR}
      MAX_FILE_SIZE: ${MAX_FILE_SIZE}
    ports:
      - "3001:3001"
    volumes:
      - ./netzero-server/files:/app/files
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - netzero-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Chat Server
  netzero-chat-server:
    build:
      context: ./netzero-chat-server
      dockerfile: Dockerfile
    container_name: netzero-chat-server
    restart: unless-stopped
    environment:
      DEPLOYMENT_MODE: ${DEPLOYMENT_MODE:-development}
      NODE_ENV: ${DEPLOYMENT_MODE:-development}
      # Development variables
      DEV_CHAT_DB_HOST: ${DEV_CHAT_DB_HOST}
      DEV_CHAT_DB_PORT: ${DEV_CHAT_DB_PORT}
      DEV_CHAT_DB_USER: ${DEV_CHAT_DB_USER}
      DEV_CHAT_DB_PASSWORD: ${DEV_CHAT_DB_PASSWORD}
      DEV_CHAT_DB_NAME: ${DEV_CHAT_DB_NAME}
      DEV_CHAT_JWT_SECRET: ${DEV_CHAT_JWT_SECRET}
      DEV_CHAT_JWT_EXPIRES_IN: ${DEV_CHAT_JWT_EXPIRES_IN}
      DEV_OPENAI_API_KEY: ${DEV_OPENAI_API_KEY}
      # Production variables
      PROD_CHAT_DB_HOST: ${PROD_CHAT_DB_HOST}
      PROD_CHAT_DB_PORT: ${PROD_CHAT_DB_PORT}
      PROD_CHAT_DB_USER: ${PROD_CHAT_DB_USER}
      PROD_CHAT_DB_PASSWORD: ${PROD_CHAT_DB_PASSWORD}
      PROD_CHAT_DB_NAME: ${PROD_CHAT_DB_NAME}
      PROD_CHAT_JWT_SECRET: ${PROD_CHAT_JWT_SECRET}
      PROD_CHAT_JWT_EXPIRES_IN: ${PROD_CHAT_JWT_EXPIRES_IN}
      PROD_OPENAI_API_KEY: ${PROD_OPENAI_API_KEY}
      # Shared variables
      CHAT_PORT: ${CHAT_PORT}
      API_PREFIX: ${API_PREFIX}
      API_VERSION: ${API_VERSION}
    ports:
      - "3004:3004"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - netzero-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3004/health"]
      interval: 30s
      timeout: 10s
      retries: 3

 
networks:
  netzero-network:
    driver: bridge
